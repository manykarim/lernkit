"""Version metadata. Single source of truth for the API build version."""

from __future__ import annotations

__version__ = "0.0.0"

# Build metadata populated by CI; falls back to "dev" for local runs.
BUILD_SHA = "dev"
BUILD_TIMESTAMP = "dev"
