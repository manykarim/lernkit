{
  "name": "@lernkit/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared build and lint config for the Lernkit monorepo.",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./tsconfig.base": "./src/tsconfig.base.json"
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'no build'",
    "test": "echo 'no tests'",
    "typecheck": "echo 'no typecheck'",
    "clean": "echo 'no clean'"
  }
}
